these files are used by unittest in test.py

typically a unittest should be self contained
means all data it needs should be in the unittest
but the html code is many lines long
so put html code is in separate files

bonus: easy comparing using diff tools
